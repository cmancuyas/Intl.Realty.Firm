@using Intl.Realty.Firm.Models.Models.ViewModel.AccountVM

@model AccountViewModel
@{
    ViewData["Title"] = "Login";
    Layout = "_Layout";
}

<container>
    @Html.HiddenFor(m => m.AccountMode)
    <div class="row m-0 p-0 page-login">
        <div class="col-md-6 min-vh-100 d-flex flex-column align-items-center justify-content-center left-part">
     @*        <span class="text-white ms-3 position-relative top-neg-10">International Realty Firm</span>
            <img src="~/files/img/logo-irf-default.jpg" alt="DENR Logo" class="img-fluid" />
            <span class="text-white position-relative top-10">©International Realty Firm</span> *@
        </div>
        <div class="col-md-6 d-flex flex-column align-items-center justify-content-center right-part">
        @*     <div class="text-center mb-md-4">
                <img src="~/files/img/logo-irf-default.jpg" alt="INTLRealtyLogo" class="img-fluid" />
            </div> *@
            <h3 class="fw-bolder text-center mb-md-5">
                International Realty Firm
            </h3>
            <div class="w-50 mb-md-2">
                @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                {
                    <div class="form-group mb-4">
                        <label class="fw-bolder" for="username">Email Address</label>
                        @Html.TextBoxFor(m => m.Username, new { @class = "form-control", @id = "email", @name = "email", @placeholder = "Please enter email" })
                        <span asp-validation-for="Username" class="text-danger fst-italic"></span>
                    </div>
                    <div class="form-group mb-1">
                        <label class="fw-bolder" for="password">Password</label>
                        <div class="input-group">
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control", @id = "password", @name = "password", @placeholder = "Please enter password" })
                            <button class="btn" type="button" id="password-toggle">
                                <i data-feather="eye" type="button"></i>
                            </button>
                        </div>
                        <span asp-validation-for="Password" class="text-danger fst-italic"></span>
                    </div>
                    <div class="d-flex justify-content-end mb-md-5">
                        <a href="#forgotPassword" data-bs-toggle="modal" class="text-decoration-none cta-forgot-password">Forgot Password?</a>
                    </div>

                    <input type="submit" value="Sign in" class="btn btn-primary w-100" />
                }

            </div>
            <div class="d-flex gap-1">
                <span class="text-secondary">Don't have an account?</span><a href="@Url.Action("SignUp", "Account")" class="text-decoration-none cta-sign-up">Sign Up</a>
            </div>
            @await Html.PartialAsync("ForgotPasswordPartialView.cshtml", Model)
        </div>
    </div>
</container>
<script src="https://code.jquery.com/jquery-3.7.1.js" integrity="sha256-eKhayi8LEQwp4NKxN+CfCh+3qOVUtJn3QNZ0TciWLP4=" crossorigin="anonymous"></script>
@* <script src="~/lib/jquery/jquery.min.js" asp-append-version="true" type="text/javascript"></script> *@

<script>
    $(document).ready(function () {
        feather.replace();

        $("#password-toggle").click(function () {
            var passwordInput = $("#password");
            var passwordToggle = $(this).find("i");

            if (passwordInput.attr("type") === "password") {
                passwordInput.attr("type", "text");

            } else {
                passwordInput.attr("type", "password");
            }
        });

        var accountMode = $('#AccountMode').val();
        if (accountMode == 'RESET') {
            $("#forgotPassword").modal("show");
        }
        else if (accountMode == 'RESET_REQUEST') {
            $("#forgotPassword").modal("hide");
            $("#successModal").modal("show");
        }
    });
</script>